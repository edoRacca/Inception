version: '3.2'

services:
  nginx:
    image: nginx
    build: ./requirements/nginx
    container_name: nginx
    ports:
      - "443:443" # HTTPS
    restart: always
  wordpress:
    image: wordpress
    build: ./requirements/wordpress
    container_name: wordpress
    ports:
      - "9000:9000"
    restart: always